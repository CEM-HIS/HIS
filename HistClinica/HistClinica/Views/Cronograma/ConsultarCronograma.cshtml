
@model IEnumerable<HistClinica.DTO.CronogramaDTO>

<div class="modal-dialog modal-lg" role="document">
	<div class="modal-content">
		<div class="modal-header">
			<h2 class="modal-title text-center" id="exampleModalLabel1">Consultar cronograma</h2>
			<button type="button" class="close" data-dismiss="modal" aria-label="Close">
				<span aria-hidden="true">&times;</span>
			</button>
		</div>
		<div class="container">
			<div class="col-sm-12 pt-3">


				@using (Html.BeginForm())
				{
					<div class="row pb-2">
						<div class="col-sm-4">
							<div class="row">
								<div class="col-sm-4">
									<label>Medico</label>
								</div>

								<div class="col-sm-8">
									@Html.DropDownList("idmedico", new SelectList(ViewBag.listamedicos, "idMedico", "nombres"), "Seleccione", new { @class = "form-control" })
								</div>
							</div>
						</div>
						<div class="col-sm-4">
						</div>
						<div class="col-sm-4">

						</div>
					</div>
				}

				<div class="row pt-3">
					<div class="col-sm-12">
						<table class="table table-bordered table-hover text-center" id="consultacronogrid">
							<thead>
								<tr>
									<th>Id</th>
									<th>Medico</th>
									<th>Fecha Inicio</th>
									<th>Fecha Fin</th>
									<th>Hora Inicio</th>
									<th>Hora fin</th>
									<th>Estado</th>
								</tr>
							</thead>
							<tbody>
								@foreach (var item in Model)
								{
								<tr>
									<td>@item.idProgramMedica</td>
									<td>@item.medico</td>
									<td>@item.fechaInicio</td>
									<td>@item.fechaFin</td>
									<td>@item.horaInicio</td>
									<td>@item.horaFin</td>
									<td>@item.descripcionEstado</td>
								</tr>
								}
							</tbody>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>